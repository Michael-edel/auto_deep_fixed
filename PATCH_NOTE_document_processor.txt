# NOTE (для Cursor/ручной вставки):
# В вашем core/document_processor.py сейчас есть защита:
#   ValueError: 'invoices' является директорией, а не файлом.
# Это правильно. Мы добавили обработку папки в main.py, чтобы не трогать процессор.
# Поэтому core/document_processor.py менять не обязательно.
#
# Если вы всё-таки хотите, чтобы process_file принимал и папку, можно добавить метод
# process_path(), но это уже отдельный рефакторинг.
